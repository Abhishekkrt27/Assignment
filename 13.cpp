//13. Program to demonstrate logical operators.

#include <iostream>
using namespace std;

int main() 
{
    int p = 0, q = 1;
    
    cout << "p && q: " << (p && q) << '\n';
    cout << "p || q: " << (p || q) << '\n';
    cout << "!p: " << (!p) << '\n';
   
    return 0;
}